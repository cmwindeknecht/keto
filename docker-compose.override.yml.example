# docker-compose.override.yml.example
# Copy this to docker-compose.override.yml for local customizations
# This file is automatically loaded by docker-compose and is gitignored

version: '3.8'

services:
  # Example: Expose additional ports
  # postgres:
  #   ports:
  #     - "5433:5432"  # Alternative port

  # Example: Mount additional volumes
  # backend-fastapi:
  #   volumes:
  #     - ./local-data:/data

  # Example: Override environment variables
  # backend-go:
  #   environment:
  #     - LOG_LEVEL=debug

  # Example: Enable debugging ports
  # backend-fastapi:
  #   ports:
  #     - "5678:5678"  # Python debugger
  #   command: python -m debugpy --listen 0.0.0.0:5678 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
